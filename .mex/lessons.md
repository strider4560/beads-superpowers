# Lessons

- lesson: The session-model tier wall is GONE (agent-authority rework, 2026-08-21): no gate or hook reads session model/size/effort. `tier-gate.sh --stage` = install preflight (version handshake + integrity + great_cto floor; no --assert, no exit 4). `pipeline-guard` = install integrity + Rule D (state dir + install surface) + Rule S (subagents, keyed on payload `agent_id`, cannot mutate beads — reads and `bd note` excepted — write mex, or edit plans/, .internal/plans|specs). Orchestrating session unrestricted. tier-map.json is dispatch economy only. Evidence: this session, live subagent probe.
- lesson: Claude Code puts every dispatched subagent in its OWN worktree (`.claude/worktrees/agent-<id>`, based on the session's start commit) and hard-denies git ops targeting any other path, so implementing-epics' caller-created `.worktrees/group-N` are unreachable — group branches sit at 0 commits while all agents report success. Cherry-pick each agent commit onto its intended branch and re-verify. Evidence: epic beads-superpowers-myj.1, all 5 implementers + 2 fix rounds; bead beads-superpowers-6gl.
- lesson: pipeline-guard Rules D and S match the raw Bash command TEXT, so a read-only ls/cat/grep — or a bare mention inside a quoted pattern — is denied at exit 2 though nothing protected is touched. Ruled intended (fail-closed, 2026-08-22). It blocked the controller storing a review report and killed 2 epic reviewers mid-run. Use the Read tool or reword; never retry variants. Evidence: beads beads-superpowers-fpi, -9q1.
- lesson: Deep reviewer/implementer subagents die mid-work without emitting — 4 of 5 epic reviewers stopped near 100k tokens / 50 tool calls, last message a fragment. Dispatch with an explicit budget rule (read, 2-3 focused checks, then emit) and they return: round 2 ran 33-52k with 5 of 5 emitting. Resume a dead one with "emit now from what you have". Evidence: epic beads-superpowers-myj.1 rounds 1-2.
